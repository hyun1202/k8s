apiVersion: v1
kind: Service
metadata:
  labels:
    app: myapp
  name: myapp-svc
spec:
  ports:
  - nodePort: 30880
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: myapp  # label로 선택
  type: NodePort